<h1> Cart#Show </h1>
  You have selected <%= @cart.line_items.count %> items.
  <br>
  <% for line_item in @cart.line_items %>
      <%= image_tag line_item.item.image %>
        <%= line_item.item.name %>
        <%= link_to 'Remove', line_item_path(line_item.item, @item), method: :delete, data: {confirm:' Ya sure?'} %>
  <% end %>
  
      Total: (add price field later)
    

<p>
  <%= link_to "Continue Shopping", items_path %> |
  <%= link_to "Checkout", new_order_path %>
</p>